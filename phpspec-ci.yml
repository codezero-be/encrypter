suites:
  codezero_suite:
    namespace: CodeZero\Encrypter
    src_path: src
    spec_path: .
    spec_prefix: spec
    psr4_prefix: CodeZero\Encrypter

extensions:
  - PhpSpec\Extension\CodeCoverageExtension

code_coverage:
  format:
    - html
    - clover
  output:
    html: coverage
    clover: coverage.clover
